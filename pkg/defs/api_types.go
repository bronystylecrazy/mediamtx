package defs

import (
	"github.com/bluenviron/mediamtx/internal/defs"
)

// APIError is a generic error.
type APIError = defs.APIError

// APIPathConfList is a list of path configurations.
type APIPathConfList = defs.APIPathConfList

// APIPathSourceOrReader is a source or a reader.
type APIPathSourceOrReader = defs.APIPathSourceOrReader

// APIPath is a path.
type APIPath = defs.APIPath

// APIPathList is a list of paths.
type APIPathList = defs.APIPathList